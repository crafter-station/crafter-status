{
	"name": "crafter-status",
	"version": "0.2.0",
	"private": true,
	"description": "WhatsApp group status: CLI, ingestor, web dashboard and MCP server",
	"license": "MIT",
	"type": "module",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"cli": "bun run --cwd apps/cli dev",
		"web": "bun run --cwd apps/web dev",
		"web:build": "bun run --cwd apps/web build",
		"ingestor": "bun run --cwd apps/ingestor dev",
		"db:generate": "bun run --cwd packages/db generate",
		"db:push": "bun run --cwd packages/db push",
		"db:studio": "bun run --cwd packages/db studio",
		"check": "biome check .",
		"check:fix": "biome check --write .",
		"typecheck": "bun run --cwd packages/db typecheck && bun run --cwd packages/core typecheck && bun run --cwd apps/ingestor typecheck && bun run --cwd apps/cli typecheck && bun run --cwd apps/web typecheck"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.0",
		"@types/bun": "latest",
		"typescript": "^5.9.0"
	},
	"engines": {
		"bun": ">=1.2.0"
	}
}
